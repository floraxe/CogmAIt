# Web框架核心
fastapi
uvicorn[standard]
pydantic>=2.11.7
python-multipart
starlette
httpx
sse-starlette

# 数据库
sqlalchemy
pymysql
mysql-connector-python

# 安全认证
python-jose[cryptography]
passlib[bcrypt]
email-validator

# 异步和文件
aiohttp
aiofiles
python-dotenv
pyyaml

# 日志
loguru

# AI提供商
anthropic
google-generativeai

# 知识图谱
neo4j
neo4j-graphrag>=1.0.0

# 文本处理
langchain-text-splitters

# 数据处理
pandas
numpy

# 文件和对象存储
minio>=7.2.0

# 数据库异步支持
asyncpg
aiomysql

# 工具服务
fastmcp>=0.1.0
tavily-python

# ==========================================
# 以下为可选依赖（注释掉，需要时再安装）
# ==========================================

# 向量搜索（大约1GB+，需要时再装）
# faiss-cpu
# sentence-transformers
# torch
# scikit-learn

# 向量数据库（可选）
# pymilvus

# 本地模型支持（复杂，需要时再装）
# modelscope

# PDF高级处理（Windows安装困难，需要时再装）
# magic_pdf
# mineru[core]

# PostgreSQL支持（如果不用PG数据库）
# psycopg2-binary

# 其他工具
# watchdog
# cachetools
# pillow
# openpyxl
# python-docx
# pypdf

# 数据库迁移工具（项目未使用）
# alembic

# 旧依赖包（已不需要）
# bcrypt  # passlib已包含
# langchain  # 只用langchain-text-splitters
# langchain-community
# langchain-openai
# google-genai  # 用google-generativeai即可